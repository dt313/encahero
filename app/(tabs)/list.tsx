import { Text } from 'react-native';

import { SafeAreaView } from 'react-native-safe-area-context';

function List() {
    return (
        <SafeAreaView>
            <Text>List</Text>
        </SafeAreaView>
    );
}

export default List;
