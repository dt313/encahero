import { Text } from 'react-native';

import { SafeAreaView } from 'react-native-safe-area-context';

function Quiz() {
    return (
        <SafeAreaView>
            <Text>Quiz</Text>
        </SafeAreaView>
    );
}

export default Quiz;
